import { ICircularCard } from './CircularCard'
import { step1Icon } from 'public/icons'

const base: ICircularCard = {
    title: 'Some title',
    icon: step1Icon
}

export const mockCircularCardProps = {
    base
}
